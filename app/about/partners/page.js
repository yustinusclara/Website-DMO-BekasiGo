// /about/partners is used in the footer nav. Rather than duplicate content, we
// simply re-export the top-level /partners page — one canonical source of truth.
export { default, metadata } from '@/app/partners/page'
